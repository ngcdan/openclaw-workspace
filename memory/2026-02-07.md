# 2026-02-07

- Sent daily backlog snapshot email via Gmail API OAuth.
- Noted that automation scripts should live in OpenClaw workspace
- Created canonical script: `/Users/nqcdan/.openclaw/workspace/automation/gmail/send-backlog-snapshot.mjs` with template + RFC2047 subject encoding.
- Workflow: when Dan drops a chat screenshot image with no extra text, auto-archive it into wiki inbox (`/Users/nqcdan/dev/wiki/notes/00_inbox/` + `assets/YYYY-MM-DD/`), create a short md note embedding the image, then commit+push.
